# Backend API URL (required on Vercel before deploy)
# Use either variable name — no trailing slash, no /api suffix
# Example: https://your-api.onrender.com
BACKEND_URL=
VITE_API_BASE_URL=http://localhost:8000
